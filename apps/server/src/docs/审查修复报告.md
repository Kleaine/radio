# 代码审查修复报告

> 项目：Radio — AI 音乐电台
> 范围：分工5（AI + 外部 API）交付物
> 审查日期：2026-05-31
> 工具：双 Agent 并行审查 × 2 轮

---

## 审查概况

| | 第一轮 | 第二轮 | 合计 |
|---|---|---|---|
| 发现问题 | 12 | 6 | 18 |
| 已修复 | 12 | 6 | 18 |
| 遗留 | 0 | 0 | 0 |

---

## 第一轮修复详情

### 缺陷类

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| 1 | `memory-writer.ts:38` | `if (!filePath) return` 死守卫，`path.join()` 永远返回字符串，守卫永不触发 | 改为先检查 `filename = FILE_MAP[entry.file]`，为 `undefined` 时 return |
| 2 | `llm.service.ts` | LLM 流式调用无超时，网络卡住会无限挂起 | `callDoubaoStream` 和 `callDoubao` 加 `AbortController`，默认 60s 超时 |
| 3 | `weather.service.ts:61` | `Number(undefined) ?? 0` 返回 NaN（`??` 不过滤 NaN） | 改为 `current?.temp_C ? Math.round(Number(current.temp_C)) : 0` |
| 4 | `weather.service.ts:71` | 模块级 `cachedWeather` 变量，多实例共享，不同城市之间数据泄漏 | 移入 `createWeatherService` 工厂函数闭包内 |

### 设计类

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| 5 | 6 个 `.ts` | 五种不同的导出模式：单例、裸函数、工厂函数、类、默认导出混用 | 统一为工厂函数：`createLlmService`、`createContextService`、`createWeatherService`、`createCalendarService`、`createMemoryWriter` |
| 6 | `llm.service.ts` | `ChatSong` 缺 `id` 和 `cover` 字段，前端无法渲染歌曲卡片 | 新增可选字段 `id?: string` 和 `cover?: string` |
| 7 | `llm.service.ts` / `calendar.service.ts` | `ScheduleEntry` 用 `{time, title}`，`CalendarEvent` 用 `{start, end, title}`，不兼容 | 统一为 `{start, end, title}`，`parseReply` 内兼容 LLM 输出 `time` 的旧格式 |
| 8 | `llm.service.ts` | 只有流式 `chatStream`，无后台调度可用的非流式方法 | 新增 `chat()` 非流式方法 |
| 9 | `memory-writer.ts` / `calendar.service.ts` | `process.cwd()` 硬编码路径，从子目录启动会解析到错误位置 | 路径改为工厂函数的可配置参数，`dataDir` / `userDir` / `systemPromptPath` 均可传入 |

### 文档类

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| 10 | `项目架构说明.md` | 声称"分工2 对着 `music.service.interface.ts` 实现 `QQMusicService`"，但代码已实现 | 改为 `QQMusicService` 和 `MockMusicService` 均已实现，分工2 直接使用 |
| 11 | `项目架构说明.md` | 目录树引用了不存在的 `package.json` 和 `tsconfig.json` | 删除引用，加脚注"由分工2 搭建项目骨架时创建" |
| 12 | `API协同流程.md` | 旧方法名：`llmService.chat()`、`writeMemories()`、`readAllProfiles()` | 更新为新方法名：`llmService.chatStream()`、`memoryWriter.writeAll()`、`memoryWriter.readAll()` |

---

## 第二轮修复详情

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| 13 | `weather.service.ts:61` | `Number(undefined) ?? 0` 返回 NaN（同第3条，确认修复） | 已在第一轮修复 |
| 14 | `weather.service.ts:71` | 模块级 `cachedWeather` 跨实例泄漏（同第4条，确认修复） | 已在第一轮修复 |
| 15 | `memory-writer.ts:40` | LLM 返回的 memory 条目缺 `add` 字段时，`entry.add.trim()` 抛 TypeError | `if (!entry.add.trim())` 改为 `if (!entry?.add?.trim())` |
| 16 | `plan-system.md:58` | prompt 让 LLM 输出 `{"time": "HH:MM"}`，但代码期望 `{"start": "HH:MM"}` | prompt 改为 `{"start": "HH:MM", "end": "HH:MM", "title": "事项"}` |
| 17 | `README.md` | 写 `pnpm install` 和 `pnpm dev`，但仓库无 `package.json`，新人会直接撞墙 | 顶部加醒目标注：当前仅 service 层代码，工程配置文件由其他分工成员提供 |
| 18 | `API协同流程.md` | 数据流图仍引用旧方法名（确认修复） | 已在第一轮修复 |

---

## 当前代码状态

- **0** 阻塞性 Bug
- **0** 崩溃风险
- **6** 个 service 全部统一为工厂函数模式
- **跨文件 import 全部正确解析**（第二轮审查逐条验证）
- **类型定义一致**（ScheduleEntry ↔ CalendarEvent ↔ plan-system.md）
- **文档与代码一致**（无矛盾引用）
