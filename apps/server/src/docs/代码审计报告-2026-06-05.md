# Radio AI 音乐电台 — 代码审计报告

> 审计日期：2026-06-05
> 目标：`radio-repo/` 全项目（master 后端 + feat/front-player 前端）
> 技术栈：TypeScript Express 后端 + vanilla JS 前端 + SQLite + Python TTS
> Agent：5 个（前后端契约、数据流完整性、错误处理/安全、架构/代码质量、配置/持久化）

---

## 摘要

| 严重度 | 数量 | 关键领域 |
|---|---|---|
| Critical | 3 | JWT 硬编码密钥、dispatch 无鉴权、前端零对接 dispatch |
| High | 5 | 日程覆盖破坏、TTS 端口冲突、dispatch 缺 TTS、chat 丢 items[]、initServices 重复 |
| Medium | 8 | 播放器无持久化、WAV 无清理、天气缓存失效、文件非原子写入、飞书过度设计 |

---

## Critical（立即修复）

| # | 问题 | 文件:行 | 影响 |
|---|---|---|---|
| 1 | JWT 密钥硬编码 `radio-dj-secret-2026`，写在 `.env.example` 中公开可见 | `auth.ts:6`, `feishu.routes.ts:10` | 任何人可伪造 token，绕过全部鉴权 |
| 2 | `/api/dispatch` 无 authMiddleware，userId 永远 undefined，播放历史永远为空 | `dispatch.routes.ts:107,175` | AI 永远无法学习用户偏好，个性化完全失效 |
| 3 | 前端 `app.js` 零对接 `/api/dispatch`，无文本输入、无 SSE 流式、无 items[] 渲染 | `app.js`（全文） | 整个 dispatch 子系统成死代码，核心链路断裂 |

---

## High / P1（本周修复）

### 前后端契约断裂

| # | 问题 | 文件 |
|---|---|---|
| 4 | 前端只调 `/api/chat`（语音），没有 `/api/dispatch` 文本输入 | `app.js` |
| 5 | chat 路由只取 firstTtsItem + firstSong，丢弃 PlanResponse 其余 items[] | `chat.routes.ts:158-171` |
| 6 | dispatch 路由 `enrichItems()` 缺 `ttsService`，所有 DJ 语音静音 | `dispatch.routes.ts:193` |

### 数据破坏

| # | 问题 | 文件 |
|---|---|---|
| 7 | `updateEvents()` 完全**覆盖**文件而非合并，AI 新增一条日程会销毁原有全部日程 | `calendar.service.ts:64-69` |

### 架构重复

| # | 问题 | 文件 |
|---|---|---|
| 8 | `initServices` 模式在 `chat.routes.ts`、`dispatch.routes.ts`、`scheduler/index.ts` 三处重复约 120 行 | 3 个文件 |

---

## Medium / P2（计划修复）

| # | 问题 | 文件:行 |
|---|---|---|
| 9 | Player 状态纯内存，服务重启丢失，多用户共享同一状态 | `player.routes.ts:23-32` |
| 10 | TTS 端口不一致：代码默认 8000，TTS 文档声称 8008 | `chat.routes.ts:92` vs `TTS_START.md:75` |
| 11 | `apps/tts/outputs/` WAV 文件无清理机制，持续增长 | `chat.routes.ts:107-116` |
| 12 | 天气缓存每实例独立，调度器的 30min 预热任务无效 | `weather.service.ts:74-76` |
| 13 | `memory-writer.ts` 和 `calendar.service.ts` 文件写入非原子，并发可能损坏 | `memory-writer.ts:52`, `calendar.service.ts:69` |
| 14 | ASR 永远返回固定 Mock 字符串 | `chat.routes.ts:83-88` |
| 15 | 调度器生成早安计划后仅 console.log，从不推送 | `scheduler/index.ts:45-47` |
| 16 | `skipped` 列从未被写入，`recentSkips` 上下文永远为空 | 全局 |

---

## 修复路线图

| 阶段 | 范围 | 估改文件 |
|---|---|---|
| P0（紧急） | JWT、dispatch 鉴权、前端补 dispatch 对接 | ~3 个 |
| P1（本周） | 日程合并修复、dispatch 补 TTS、initServices 去重、chat 路由处理全部 items[] | ~4 个 |
| P2（后续） | 播放器持久化、TTS 端口统一、WAV 清理、文件原子写、ASR 接入 | ~6 个 |

---

## 当前状态：前后端不对接

后端有 `/api/dispatch`（SSE 流式 + 三层分发）但前端从未调用。
前端有 `/api/chat`（语音录音）但 ASR 是 Mock。
两条核心链路各断一个端点。**项目能启动但用户无法完整体验"说话→AI 回复→播放"的闭环。**
